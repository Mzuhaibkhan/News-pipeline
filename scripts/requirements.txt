# ── Core Framework ──
fastapi>=0.100.0,<1.0
uvicorn[standard]>=0.23.0,<1.0
gunicorn>=21.2.0,<23.0
orjson>=3.9.0,<4.0
slowapi>=0.1.9,<1.0
email-validator>=2.0.0,<3.0

# ── Database ──
pymongo[srv]>=4.6,<5.0
motor>=3.3.0,<4.0

# ── HTTP & Networking ──
requests>=2.31,<3.0
httpx>=0.27.0,<1.0
certifi>=2024.0.0

# ── Caching & Messaging ──
redis>=5.0.0,<6.0
arq>=0.25.0,<1.0

# ── NLP & Text Processing ──
textblob>=0.18,<1.0
rake-nltk>=1.0,<2.0
nltk>=3.8,<4.0
langdetect>=1.0,<2.0

# ── RSS & Feed Parsing ──
feedparser>=6.0,<7.0

# ── Email & Templates ──
jinja2>=3.1.0,<4.0
itsdangerous>=2.1.0,<3.0

# ── Real-Time Streaming ──
sse-starlette>=1.6.0,<3.0

# ── Observability ──
prometheus-fastapi-instrumentator>=6.0.0,<8.0

# ── Configuration ──
python-dotenv>=1.0,<2.0
